import {Validators} from '@angular/forms';

export class ProductItem {
  id: number;
  title: string;
  image: string;
  description: string;
  width: string;
  length: string;
  depth: string;
  color: string;
  features: string;
  originalPrice: string;
  newPrice: string;
}
